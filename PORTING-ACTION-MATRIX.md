# 动作覆盖继续审计

生成日期：2026-09-12。上一个提交 `623f565` 完成了 `zone_entity` 动作家族。

- 当前注册动作：129
- 上游注册动作：188
- 静态缺失动作：62
- 本批已注册 zone_entity 变体：11/11

## zone_entity 参数顺序证据

上游 `OpGetEntitiesBy` 的源码中出现的栈弹出调用：

```text
未提取到 pop 调用；需要人工检查上游文件。
```

当前 1.12.2 实现按 `radius` 后 `position` 的顺序弹出；若上游动作语义要求相反，下一次运行时测试应修正该顺序。

## 下一批候选

missing 数量较大，但 `read/write`、`compare_block` 和高级 eval 动作依赖 1.20.1 的新环境/数据类型。优先顺序应为：
1. 先补 1.12.2 已有 Iota 类型可表达的纯堆栈动作；
2. 再补实体查询/方块查询，并为旧版本数据结构建立明确兼容类型；
3. 最后处理 read/write、eval/cc、Akashic 和高级 interop。

