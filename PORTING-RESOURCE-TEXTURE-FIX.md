# 1.12.2 物品纹理修复记录

生成日期：2026-09-12。修复目标是模型引用的 PNG 不存在，以及上游 Hex 图案相关纹理未进入 1.12.2 资源包。

- 检查 item 模型：6
- 本次补入 PNG：0
- 仍无法从上游定位的模型纹理：0
- 上游图案纹理候选：0

## 本次导入

无；模型引用的纹理已经存在。

## 未解决引用

无。

## 验证说明

- 模型 JSON 使用 1.12.2 的 `item/generated` 资源格式，纹理路径按 `assets/<namespace>/textures/<path>.png` 解析。
- 该记录只处理资源文件；本地化仍通过 `.lang` 键，不把文本写入 Java/Kotlin。
- 客户端必须使用本次构建产物重新启动后，才能最终确认纸张材质问题是否消失。

